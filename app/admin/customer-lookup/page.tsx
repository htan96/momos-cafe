import PlatformPlaceholderCard from "@/components/platform/PlatformPlaceholderCard";

export default function AdminCustomerLookupPage() {
  return (
    <PlatformPlaceholderCard
      title="Customer lookup"
      description="Search by email or phone once directory indexes are synced from Cognito and Square profiles."
    />
  );
}
