import PlatformPlaceholderCard from "@/components/platform/PlatformPlaceholderCard";

export default function AccountSettingsAddressesPage() {
  return (
    <PlatformPlaceholderCard
      title="Saved addresses"
      description="Shipping and event locations you reuse — validated for delivery and catering logistics."
    />
  );
}
