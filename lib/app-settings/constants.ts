/** Prisma `AppSetting.settingId` for retail storefront (`/shop`, shop lines in checkout). */
export const APP_SETTING_SHOP_ENABLED = "ShopEnabled";
/** Prisma `AppSetting.settingId` for menu-driven flows (`/menu`, `/order`, kitchen lines). */
export const APP_SETTING_MENU_ENABLED = "MenuEnabled";
