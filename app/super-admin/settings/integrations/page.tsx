import PlatformPlaceholderCard from "@/components/platform/PlatformPlaceholderCard";

export default function SuperAdminSettingsIntegrationsPage() {
  return (
    <PlatformPlaceholderCard
      title="Global integrations"
      description="Cross-environment keys for observability, email relay, and third-party governance — never expose to admin-only roles."
    />
  );
}
