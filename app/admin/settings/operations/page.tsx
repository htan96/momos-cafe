import PlatformPlaceholderCard from "@/components/platform/PlatformPlaceholderCard";

export default function AdminSettingsOperationsPage() {
  return (
    <PlatformPlaceholderCard
      title="Operations defaults"
      description="Prep times, routing rules, and queue priorities — coordinates with the fulfillment and shipping tools."
    />
  );
}
