import PlatformPlaceholderCard from "@/components/platform/PlatformPlaceholderCard";

export default function AdminQueuesPage() {
  return (
    <PlatformPlaceholderCard
      title="Queues"
      description="Operational queues across channels — configure routing and ownership before high-volume events."
    />
  );
}
