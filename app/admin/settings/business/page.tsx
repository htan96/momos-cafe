import PlatformPlaceholderCard from "@/components/platform/PlatformPlaceholderCard";

export default function AdminSettingsBusinessPage() {
  return (
    <PlatformPlaceholderCard
      title="Business settings"
      description="Brand name, tax identifiers, locations, and default fulfillment policies for the operations console."
    />
  );
}
