import PlatformPlaceholderCard from "@/components/platform/PlatformPlaceholderCard";

export default function AdminCommunicationsPage() {
  return (
    <PlatformPlaceholderCard
      title="Communications"
      description="Outbound templates and message history for staff-initiated customer contact — complements transactional email workflows in code."
    />
  );
}
