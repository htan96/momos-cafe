import PlatformPlaceholderCard from "@/components/platform/PlatformPlaceholderCard";

export default function SuperAdminSettingsPlatformPage() {
  return (
    <PlatformPlaceholderCard
      title="Global platform settings"
      description="Region flags, feature toggles, and cross-tenant defaults — change with extreme care and audit logging."
    />
  );
}
