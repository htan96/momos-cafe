import PlatformPlaceholderCard from "@/components/platform/PlatformPlaceholderCard";

export default function AdminNotificationsPage() {
  return (
    <PlatformPlaceholderCard
      title="Notifications"
      description="Real-time alerts for staff (browser or mobile) — distinct from business notification settings under Admin → Settings."
    />
  );
}
