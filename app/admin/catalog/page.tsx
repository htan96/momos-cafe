import PlatformPlaceholderCard from "@/components/platform/PlatformPlaceholderCard";

export default function AdminCatalogPage() {
  return (
    <PlatformPlaceholderCard
      title="Catalog & menu"
      description="Square catalog sync, café menu visibility, and merchandising controls — extend from existing catalog sync jobs."
    />
  );
}
