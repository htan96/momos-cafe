import PlatformPlaceholderCard from "@/components/platform/PlatformPlaceholderCard";

export default function SuperAdminSettingsHealthPage() {
  return (
    <PlatformPlaceholderCard
      title="Deployment & health"
      description="Placeholder for build status, uptime probes, and version — connect CI/CD and runtime checks when deployed."
    />
  );
}
