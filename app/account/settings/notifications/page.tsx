import PlatformPlaceholderCard from "@/components/platform/PlatformPlaceholderCard";

export default function AccountSettingsNotificationsPage() {
  return (
    <PlatformPlaceholderCard
      title="Notification preferences"
      description="Choose which transactional and optional emails or texts you want — always aligned with compliance and opt-in rules."
    />
  );
}
