import PlatformPlaceholderCard from "@/components/platform/PlatformPlaceholderCard";

export default function AdminRefundsPage() {
  return (
    <PlatformPlaceholderCard
      title="Refunds"
      description="Payment provider reversals, partial refunds, and audit trails — gated to trusted operators."
    />
  );
}
