import PlatformPlaceholderCard from "@/components/platform/PlatformPlaceholderCard";

export default function AccountSettingsSecurityPage() {
  return (
    <PlatformPlaceholderCard
      title="Security"
      description="Password changes, MFA posture, and active sessions will surface here with Cognito-backed flows."
    />
  );
}
