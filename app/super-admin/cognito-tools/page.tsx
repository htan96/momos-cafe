import PlatformPlaceholderCard from "@/components/platform/PlatformPlaceholderCard";

export default function SuperAdminCognitoToolsPage() {
  return (
    <PlatformPlaceholderCard
      title="Cognito utilities"
      description="Operational shortcuts—password resets with guardrails, group membership diffs—without leaving the audited shell."
    />
  );
}
