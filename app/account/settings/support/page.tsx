import PlatformPlaceholderCard from "@/components/platform/PlatformPlaceholderCard";

export default function AccountSettingsSupportPage() {
  return (
    <PlatformPlaceholderCard
      title="Support"
      description="Prior support tickets and escalations tied to your email — integrate with your help desk when ready."
    />
  );
}
