import PlatformPlaceholderCard from "@/components/platform/PlatformPlaceholderCard";

export default function AdminOrderLookupPage() {
  return (
    <PlatformPlaceholderCard
      title="Order lookup"
      description="Recover order metadata, timelines, and fulfillment detail with guardrails mirrored from production APIs."
    />
  );
}
