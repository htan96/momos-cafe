import PlatformPlaceholderCard from "@/components/platform/PlatformPlaceholderCard";

export default function AccountInvoicesPage() {
  return (
    <PlatformPlaceholderCard
      title="Invoices & receipts"
      description="Downloadable PDFs and consolidated billing history for catering and large orders — connect to accounting export when ready."
    />
  );
}
