import PlatformPlaceholderCard from "@/components/platform/PlatformPlaceholderCard";

export default function AdminSupportPage() {
  return (
    <PlatformPlaceholderCard
      title="Support inbox"
      description="Guest issues escalated from the storefront — placeholder until a ticketing or shared-inbox integration is chosen."
    />
  );
}
