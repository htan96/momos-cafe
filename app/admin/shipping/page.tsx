import PlatformPlaceholderCard from "@/components/platform/PlatformPlaceholderCard";

export default function AdminShippingPage() {
  return (
    <PlatformPlaceholderCard
      title="Shipping & Shippo"
      description="Future home for Shippo rate shopping, label purchase, and outbound tracking numbers. No live API calls yet — wire credentials in admin integrations when ready."
    />
  );
}
