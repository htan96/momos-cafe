import PlatformPlaceholderCard from "@/components/platform/PlatformPlaceholderCard";

export default function SuperAdminCustomerLookupPage() {
  return (
    <PlatformPlaceholderCard
      title="Customer lookup"
      description="Privileged search across diners with MFA-enforced confirmations before sensitive fields unlock."
    />
  );
}
